apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: colab-pvc-libs
  namespace: colab-llm-cpu
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: premium-rwo
  resources:
    requests:
      storage: 100Gi
